# API contract

Generated by `npm run contract` in `server/`. **Do not edit by hand** — every
shape below was captured from a live response, so if something here is wrong,
the server is wrong.

The frontend and the backend share nothing but this. `web/` imports no server
code, and it should stay that way: the moment it does, the two halves cannot be
worked on separately.

- Base URL: `/api`, same origin in production.
- Auth: `Authorization: Bearer <token>` on everything except `/api/health`,
  `/api/auth/register` and `/api/auth/login`.
- Errors: `{ error: string, message: string, details?: unknown }` with a real
  status code. `message` is written to be shown to a person as-is.
- Dates are ISO strings. A bare `YYYY-MM-DD` is read as local noon, never UTC
  midnight — that distinction was a real bug once.

Probed 29 read endpoints: 29 captured.

`unknown[]` means the probe account's list was genuinely empty — no waste
history, no forecasts, no meal plan yet — not that the server does not know
the shape. Those arrays hold the same objects their non-empty siblings do.

## Reads

### `GET /api/health`

Liveness, offline mode and the expiry window.

```ts
{
  status: string;
  offlineMode: boolean;
  expiryWarningDays: number;
}
```

### `GET /api/auth/me`

Who the token belongs to.

```ts
{
  user: {
    id: string;
    email: string;
  };
}
```

### `GET /api/dashboard`

Everything the home screen needs, in one call.

```ts
{
  expiryWarningDays: number;
  inventoryCount: number;
  expiring: {
      id: string;
      name: string;
      quantity: number;
      unit: string;
      daysUntilExpiration: number;
      expiryStatus: string;
    }[];
  useItUpRecipes: {
      id: string;
      name: string;
      usesExpiring: string[];
      totalMinutes: number;
    }[];
  today: {
    totalCalories: number;
    entryCount: number;
    unknownCalorieEntries: number;
    macros: {
      protein: number;
      carbs: number;
      fat: number;
    };
    macroSplit: {
      protein: number;
      carbs: number;
      fat: number;
      hasData: boolean;
    };
    targets: {
      calories: number;
      protein: number;
      carbs: number;
      fat: number;
    };
    caloriesRemaining: number;
  };
  cookableNow: {
      id: string;
      name: string;
      servings: number;
      totalMinutes: number;
      nutrition: {
        caloriesPerServing: number;
        proteinPerServing: number;
        carbsPerServing: number;
        fatPerServing: number;
        partial: boolean;
      };
    }[];
  cookableCount: number;
  shoppingListOpenCount: number;
  staleItems: unknown[];
  waste: {
    wastedItems: number;
    perWeek: number;
    topWasted: unknown[];
    byCategory: unknown[];
  };
  ads: {
      id: string;
      slot: string;
      sponsor: string;
      headline: string;
      body: string;
      cta: string;
      label: string;
      foodReferenceId: string;
    }[];
}
```

### `GET /api/settings`

Goals, targets, diet tags, ad and notification switches.

```ts
{
  settings: {
    email: string;
    weightGoal: string;
    dailyCalorieTarget: number;
    proteinTargetGrams: number;
    carbsTargetGrams: number;
    fatTargetGrams: number;
    adsEnabled: boolean;
    autoShoppingEnabled: boolean;
    expiryWarningDays: number;
    unitSystem: string;
    dietTags: unknown[];
    notifyExpiry: boolean;
  };
  suggestions: {
    lose: {
      dailyCalorieTarget: number;
      proteinTargetGrams: number;
      carbsTargetGrams: number;
      fatTargetGrams: number;
    };
    maintain: {
      dailyCalorieTarget: number;
      proteinTargetGrams: number;
      carbsTargetGrams: number;
      fatTargetGrams: number;
    };
    gain: {
      dailyCalorieTarget: number;
      proteinTargetGrams: number;
      carbsTargetGrams: number;
      fatTargetGrams: number;
    };
    baseline: {
      lose: number;
      maintain: number;
      gain: number;
    };
  };
}
```

### `GET /api/inventory?sort=expiration`

The pantry. sort=expiration|category|name|recent.

```ts
{
  items: {
      id: string;
      quantity: number;
      unit: string;
      expirationDate: string;
      purchasedAt: string;
      daysUntilExpiration: number;
      expiryStatus: string;
      storageLocation: string;
      lowStockThreshold: null;
      isLowStock: boolean;
      isLeftover: boolean;
      food: {
        id: string;
        name: string;
        brand: null;
        barcode: null;
        category: string;
        source: string;
        defaultUnit: string;
        caloriesPerUnit: number;
        countsAs: null;
      };
      caloriesRemaining: number;
      macrosRemaining: {
        protein: number;
        carbs: number;
        fat: number;
      };
    }[];
}
```

### `GET /api/inventory/expiring`

Only what is about to go off.

```ts
{
  days: number;
  items: {
      id: string;
      quantity: number;
      unit: string;
      expirationDate: string;
      purchasedAt: string;
      daysUntilExpiration: number;
      expiryStatus: string;
      storageLocation: string;
      lowStockThreshold: null;
      isLowStock: boolean;
      isLeftover: boolean;
      food: {
        id: string;
        name: string;
        brand: null;
        barcode: null;
        category: string;
        source: string;
        defaultUnit: string;
        caloriesPerUnit: number;
        countsAs: null;
      };
      caloriesRemaining: number;
      macrosRemaining: {
        protein: number;
        carbs: number;
        fat: number;
      };
    }[];
}
```

### `GET /api/inventory/stale`

Bought and never touched.

```ts
{
  items: unknown[];
}
```

### `GET /api/inventory/cmt8x09170002xn9vjeiv27it`

One pantry item.

```ts
{
  item: {
    id: string;
    quantity: number;
    unit: string;
    expirationDate: string;
    purchasedAt: string;
    daysUntilExpiration: number;
    expiryStatus: string;
    storageLocation: string;
    lowStockThreshold: null;
    isLowStock: boolean;
    isLeftover: boolean;
    food: {
      id: string;
      name: string;
      brand: null;
      barcode: null;
      category: string;
      source: string;
      defaultUnit: string;
      caloriesPerUnit: number;
      countsAs: null;
    };
    caloriesRemaining: number;
    macrosRemaining: {
      protein: number;
      carbs: number;
      fat: number;
    };
  };
}
```

### `GET /api/recipes?limit=3`

Ranked search. q, limit, maxMinutes, maxCalories, maxGaps, tag, mine.

```ts
{
  dietHidden: number;
  dietTags: unknown[];
  recipes: {
      id: string;
      name: string;
      description: string;
      servings: number;
      canMakeNow: boolean;
      counts: {
        ok: number;
        short: number;
        missing: number;
        unknown_conversion: number;
      };
      gaps: number;
      totalMinutes: number;
      difficulty: string;
      tags: string[];
      nutrition: {
        caloriesPerServing: number;
        proteinPerServing: number;
        carbsPerServing: number;
        fatPerServing: number;
        partial: boolean;
      };
      usesExpiring: string[];
      reasons: string[];
      isMine: boolean;
      source: string;
      missing: unknown[];
    }[];
}
```

### `GET /api/recipes/almost?limit=2`

One or two ingredients away, with the gaps named.

```ts
{
  recipes: {
      id: string;
      name: string;
      description: string;
      servings: number;
      totalMinutes: number;
      nutrition: {
        caloriesPerServing: number;
        proteinPerServing: number;
        carbsPerServing: number;
        fatPerServing: number;
        partial: boolean;
      };
      gaps: number;
      missing: { … }[];
    }[];
}
```

### `GET /api/recipes/cmt7mc9zu000ixnxqwqpl4mi7`

One recipe, matched against your pantry.

```ts
{
  recipe: {
    id: string;
    name: string;
    description: string;
    servings: number;
    excludedIngredients: unknown[];
    canMakeNow: boolean;
    counts: {
      ok: number;
      short: number;
      missing: number;
      unknown_conversion: number;
    };
    ingredients: {
        recipeIngredientId: string;
        foodReferenceId: string;
        name: string;
        substitutes: unknown[];
        options: unknown[];
        brand: null;
        requiredQuantity: number;
        requiredUnit: string;
        status: string;
        available: number;
        shortfall: number;
        note: string;
        plan: { … };
      }[];
    totalMinutes: number;
    prepMinutes: number;
    cookMinutes: number;
    difficulty: null;
    cuisine: string;
    tags: string[];
    nutrition: {
      caloriesPerServing: number;
      proteinPerServing: number;
      carbsPerServing: number;
      fatPerServing: number;
      partial: boolean;
    };
    usesExpiring: string[];
    reasons: string[];
    isMine: boolean;
    source: string;
    instructions: string;
    sourceUrl: string;
  };
}
```

### `GET /api/recipes/cmt7mc9zu000ixnxqwqpl4mi7/cook-preview`

Exactly what a cook would deduct. Show this before POST /cook.

```ts
{
  preview: {
    id: string;
    name: string;
    description: string;
    servings: number;
    excludedIngredients: unknown[];
    canMakeNow: boolean;
    counts: {
      ok: number;
      short: number;
      missing: number;
      unknown_conversion: number;
    };
    ingredients: {
        recipeIngredientId: string;
        foodReferenceId: string;
        name: string;
        substitutes: unknown[];
        options: unknown[];
        brand: null;
        requiredQuantity: number;
        requiredUnit: string;
        status: string;
        available: number;
        shortfall: number;
        note: string;
        plan: { … };
      }[];
    totalMinutes: number;
    prepMinutes: number;
    cookMinutes: number;
    difficulty: null;
    cuisine: string;
    tags: string[];
    nutrition: {
      caloriesPerServing: number;
      proteinPerServing: number;
      carbsPerServing: number;
      fatPerServing: number;
      partial: boolean;
    };
    usesExpiring: string[];
    reasons: string[];
    isMine: boolean;
    source: string;
    instructions: string;
    servingsCooked: number;
    blocked: boolean;
    blockingReasons: string[];
    estimatedCalories: null;
  };
}
```

### `GET /api/recipes/for-food/cmt3ew9960000xns8kx3qwd49`

What you can make with one pantry item.

```ts
{
  foodReferenceId: string;
  foodName: string;
  recipes: {
      id: string;
      name: string;
      description: string;
      totalMinutes: number;
      quantity: number;
      unit: string;
      canMakeNow: boolean;
      gaps: number;
      missing: unknown[];
      nutrition: {
        caloriesPerServing: number;
        proteinPerServing: number;
        carbsPerServing: number;
        fatPerServing: number;
        partial: boolean;
      };
      isMine: boolean;
    }[];
  total: number;
}
```

### `GET /api/shopping-list`

The list, open and checked.

```ts
{
  items: {
      id: string;
      name: string;
      quantityNeeded: number;
      unit: string;
      isChecked: boolean;
      addedFrom: string;
      createdAt: string;
      foodReferenceId: null;
      food: null;
    }[];
  ads: unknown[];
}
```

### `GET /api/consumption/today`

Today’s diary, grouped so a cooked meal is one entry.

```ts
{
  date: string;
  totalCalories: number;
  macros: {
    protein: number;
    carbs: number;
    fat: number;
  };
  macroSplit: {
    protein: number;
    carbs: number;
    fat: number;
    hasData: boolean;
  };
  targets: {
    calories: number;
    protein: number;
    carbs: number;
    fat: number;
  };
  caloriesRemaining: number;
  entryCount: number;
  unknownCalorieEntries: number;
  meals: {
      slot: string;
      entries: { … }[];
      calories: number;
    }[];
  entries: {
      id: string;
      kind: string;
      name: string;
      brand: null;
      quantity: number;
      unit: string;
      calories: number;
      protein: number;
      carbs: number;
      fat: number;
      source: string;
      mealSlot: string;
      recipeId: null;
      recipeName: null;
      ingredientCount: number;
      consumedAt: string;
    }[];
}
```

### `GET /api/consumption/history?days=3`

Calories and macros per day.

```ts
{
  days: {
      date: string;
      totalCalories: number;
      protein: number;
    }[];
}
```

### `GET /api/consumption/eat-out/recent`

Things eaten out before, for one-tap repeat.

```ts
{
  recent: unknown[];
}
```

### `GET /api/reports/waste`

What was thrown away, and the pattern in it.

```ts
{
  days: number;
  wastedItems: number;
  perWeek: number;
  shareOfRemovals: number;
  topWasted: unknown[];
  byCategory: unknown[];
  byReason: {
    wasted: number;
    other_person: number;
    used_up: number;
  };
}
```

### `GET /api/reports/waste/log`

The waste log itself.

```ts
{
  entries: unknown[];
}
```

### `GET /api/planning/plan`

The meal plan.

```ts
{
  entries: unknown[];
}
```

### `GET /api/planning/plan/shortfall`

What the plan needs that you do not have.

```ts
{
  plannedMeals: number;
  missing: unknown[];
}
```

### `GET /api/planning/frequent`

Recipes cooked most often.

```ts
{
  recipes: unknown[];
}
```

### `GET /api/planning/run-out`

Staples forecast to run out soon.

```ts
{
  predictions: unknown[];
}
```

### `GET /api/planning/digest`

The daily nudge.

```ts
{
  headline: string;
  expiring: {
      name: string;
      daysUntilExpiration: number;
    }[];
  rescueRecipes: {
      id: string;
      name: string;
      uses: string[];
      totalMinutes: number;
    }[];
  runningOut: unknown[];
}
```

### `GET /api/planning/leftovers`

Portions in the fridge.

```ts
{
  leftovers: unknown[];
}
```

### `GET /api/foods/search?q=egg`

Catalog search.

```ts
{
  foods: {
      id: string;
      name: string;
      nameNorm: string;
      brand: null;
      barcode: null;
      source: string;
      externalId: null;
      category: string;
      defaultUnit: string;
      caloriesPerUnit: number;
      proteinPerUnit: number;
      fatPerUnit: number;
      carbsPerUnit: number;
      servingSizeGrams: number;
      shelfLifeDays: number;
      sponsorName: null;
      sponsorTagline: null;
      canonicalId: null;
      canonicalSource: null;
      cookedFromRecipeId: null;
      createdAt: string;
      updatedAt: string;
    }[];
}
```

### `GET /api/foods/cmt3ew9960000xns8kx3qwd49/pack`

Pack size, and whether it is known or only guessed.

```ts
{
  foodReferenceId: string;
  name: string;
  defaultUnit: string;
  grams: number;
  estimated: boolean;
  known: boolean;
}
```

### `GET /api/foods/units`

Every unit the converter understands.

```ts
{
  units: string[];
}
```

### `GET /api/ads?slot=recipes`

Demo sponsorships. Empty when ads are off.

```ts
{
  ads: {
      id: string;
      slot: string;
      sponsor: string;
      headline: string;
      body: string;
      cta: string;
      label: string;
      foodReferenceId: string;
    }[];
  adsEnabled: boolean;
}
```


## Writes

These change state, so they are described rather than called.

| Method | Path | Body | What it does |
|---|---|---|---|
| `POST` | `/api/auth/register` | `{ email, password }` | Creates an account, returns { token, user }. |
| `POST` | `/api/auth/login` | `{ email, password }` | Returns { token, user }. |
| `POST` | `/api/auth/password` | `{ currentPassword, newPassword }` | Changes your own password. |
| `POST` | `/api/inventory` | `{ foodReferenceId | name, quantity, unit, expirationDate?, storageLocation? }` | Adds a lot. |
| `PATCH` | `/api/inventory/:id` | `{ quantity?, unit?, expirationDate?, lowStockThreshold? }` | Corrects a lot. |
| `POST` | `/api/inventory/:id/consume` | `{ quantity, unit, mealSlot }` | Eat some. Writes the diary. |
| `POST` | `/api/inventory/:id/remove` | `{ reason: wasted|other_person|used_up, quantity, unit }` | Gone, but not eaten. No calories. |
| `POST` | `/api/inventory/:id/freeze` | `—` | Moves to the freezer and extends the date. |
| `DELETE` | `/api/inventory/:id` | `—` | Removes the lot entirely. |
| `POST` | `/api/recipes/:id/cook` | `{ servings?, mealSlot?, choices?, exclude?, keepServings?, swaps? }` | Atomic: deducts every ingredient and logs it, or nothing. |
| `POST` | `/api/recipes` | `{ name, instructions, servings, ingredients[] }` | Your own recipe. Owned by you. |
| `POST` | `/api/recipes/import` | `{ url }` | Imports from a link into your own book. |
| `DELETE` | `/api/recipes/:id` | `—` | Soft-deletes a recipe you added. 404 for anyone else’s. |
| `POST` | `/api/shopping-list` | `{ name, quantityNeeded, unit }` | Adds an item. |
| `PATCH` | `/api/shopping-list/:id` | `{ isChecked?, quantityNeeded? }` | Ticks or edits. |
| `POST` | `/api/shopping-list/:id/stock` | `{ quantity?, unit?, expirationDate? }` | Bought it — moves it into the pantry. |
| `POST` | `/api/shopping-list/from-recipe/:recipeId` | `{ servings? }` | Adds a recipe’s gaps. |
| `POST` | `/api/consumption/eat-out` | `{ name, calories?, mealSlot }` | Logs food you did not own. |
| `PATCH` | `/api/settings` | `{ weightGoal?, dailyCalorieTarget?, dietTags?, adsEnabled?, … }` | Updates settings. |
| `PUT` | `/api/foods/:id/counts-as` | `{ canonicalId }` | Says what a scanned product really is. |
| `POST` | `/api/foods/:id/conversions` | `{ fromUnit, toUnit, multiplier }` | Teaches a conversion, e.g. package→g. |
| `PUT` | `/api/planning/ratings/:recipeId` | `{ rating, note? }` | Rates a recipe 1–5. |

## Rules that outlive any screen

1. **Never show a cook without its preview.** `GET /cook-preview` says exactly
   what will be deducted; `POST /cook` then does it atomically. A cook that is
   short one ingredient changes nothing at all.
2. **A quantity without a unit is meaningless.** Every amount in this API is a
   `{ quantity, unit }` pair. Do not add two of them in the frontend — the
   server converts, and it refuses when it cannot.
3. **`status` on an ingredient is one of `ok | short | missing | unknown_conversion`.**
   The last one means the engine declined to guess, not that it failed. Say so.
4. **Anything owned shows `isMine`.** A recipe with `ownerId` set belongs to one
   account and 404s for everybody else.
